# 📄 RAG PDF Chatbot (LangChain + ChromaDB)

A Retrieval-Augmented Generation (RAG) chatbot that answers questions from PDF documents using **LangChain**, **ChromaDB**, and **OpenAI embeddings**.

This project loads PDFs, splits them into chunks, embeds them into a vector database, and retrieves relevant context to answer user questions accurately.

---

## 🚀 Features

- Load multiple PDF files
- Chunk documents safely
- Token-limited context building
- ChromaDB vector storage
- MMR-based retrieval
- Clean and simple CLI chatbot
- Production-style ingestion logic (one-time embedding)

---

## 📁 Project Structure

